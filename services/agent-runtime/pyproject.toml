[project]
name = "insight-agent-runtime"
version = "0.1.0"
description = "Python FastAPI and LangGraph Agent Runtime for Insight Agent Platform."
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
