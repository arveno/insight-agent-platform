# Runtime Capability Coverage

## 0. 文档定位

本文是 `#155` 后续 runtime 工作的运行能力覆盖、首轮核心动作验收边界和范围压缩边界说明。

本文不替代 `AGENTS.md`、`docs/architecture.md`、`docs/contracts.md`、`docs/runtime-lifecycle.md`、`docs/database.md`、`docs/deployment.md`。

本文不新增 contracts，不新增 API，不新增 DB migration。

本文用于后续 Issue / PR 对照运行能力覆盖、承载位置和首轮核心动作验收边界。

运行能力链路可以按阶段实现，但正式承载位不能缺失、不能绕过。

## 1. 总原则

- 运行能力链路不缺位。后续 PR 可以分阶段推进，但不能只补表面结果，不补正式承载位。
- 实现规模可以压缩。首轮验收只要求覆盖第一条真实主链，不要求一次铺满全部扩展分支。
- 范围压缩只允许压缩数量、规模、组织流程和扩展分支，不允许压缩到无法体现该能力的核心运行动作。某项能力如果本轮只完成承载位，必须明确说明核心动作由哪个后续 issue 完成；不得把空壳承载位视为该能力已经完成。
- Preview 环境可以优先。`#155` 当前允许优先使用 `Single ECS Docker Runtime` preview 环境承接真实集成验证。
- 本地完整基础设施复刻不作为当前阻塞项。本地先要求 `Docker build`、基础测试、contract check 和 smoke client。
- 数据可以 reset，但必须通过 `migration -> seed -> query verify`。不允许跳过 migration，不允许只改 seed 假装结构已经存在。
- 不能用 `mock / real` 双轨代替真实链路。可以有 test fixture、fake provider for tests、local dev adapter，但不能形成长期双主线。
- 不能用手工散操作代替仓库内脚本和事实源。数据库、部署、smoke、rollback、failure simulation 都必须逐步落回仓库入口。

## 2. 运行能力覆盖矩阵

下表用于约束后续 `#157-#165` 在同一套 runtime 主线下推进。

| 能力项                                                                                                                              | 用途说明                                                                                                                                                                        | 主要承载位                                                                                                                                                                      | 首轮核心动作验收                                                                                                                                                                                                                           | 允许压缩的范围                                                                                                           | 禁止绕过的边界                                                                                                                                  | 相关事实源                                                                                                                     | 后续关联 Issue                            |
| ----------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------- |
| `AnalysisRun Lifecycle（AnalysisRun 生命周期）`                                                                                     | 一次分析运行从创建、校验、治理、上下文绑定、规划、排队、执行、证据绑定、综合生成、校验到交付的完整状态链路。                                                                    | `AnalysisRun`、`runId`、`status`、`phase`、`RunEvent`、`ExecutionAttempt`、`LangGraph`（运行图编排框架）runtime owner。                                                         | 必须真实发生 `status / phase / RunEvent` 流转，并能查询和回放；不能只创建一个 `completed` 结果。                                                                                                                                           | 可以压缩失败分支数量、恢复分支规模和调度拓扑复杂度，不压缩真实流转动作。                                                 | 不得用 `AgentRun`、`agentRunId`、`runtimeId`、`sessionId`、`traceId` 替代正式主线。                                                             | `docs/runtime-lifecycle.md`、`docs/runtime-l3-golden-path.md`、`packages/contracts/schemas/analysis/**`                        | `#157` `#158` `#159` `#160` `#161` `#165` |
| `访问控制 / RBAC（Role-Based Access Control，基于角色的权限控制）/ ACL（Access Control List，访问控制列表）`                        | 控制哪个用户、角色、workspace 可以访问哪些数据、模型和工具。                                                                                                                    | `workspaces`、`users`、`workspace_members`、`roles`、`permissions`、Governance Policy。                                                                                         | 必须真实参与至少一次模型、工具或数据访问判断；不能只建用户、角色、权限表而没有判定动作。                                                                                                                                                   | 可以压缩角色数量、组织层级和授权流程复杂度，不压缩真实判定动作。                                                         | 不得把权限判断散落到 route、前端或无 owner 的 helper。                                                                                          | `docs/architecture.md`、`docs/database.md`、`docs/contracts.md`                                                                | `#157` `#159` `#160`                      |
| `Governance（治理）/ SQL Guard（SQL 风险控制）/ Policy（策略）`                                                                     | 对权限、SQL 风险、工具风险、敏感字段、审计进行统一约束。                                                                                                                        | governance module、`SQL Guard`、policy decision record、audit / error records、`RunEvent`。                                                                                     | 必须真实产生至少一次 allow / reject 决策；reject 必须带有 error / event / audit 记录，不能只写日志。                                                                                                                                       | 可以压缩规则数量、策略来源和审批层级，不压缩真实决策动作。                                                               | 不得让模型直接执行 SQL，不得把策略拒绝只写成日志而不回挂正式对象链。                                                                            | `AGENTS.md`、`docs/architecture.md`、`docs/runtime-lifecycle.md`、`packages/contracts/schemas/governance/**`                   | `#157` `#159` `#160` `#164`               |
| `Model Gateway（模型网关，统一管理模型 provider、路由、fallback、token、成本、延迟和错误类型）/ Provider Routing（模型供应商路由）` | 统一管理模型调用入口，负责 provider 选择、retry、fallback、token、成本、延迟和错误类型。                                                                                        | `Model Gateway`、`model_calls`、provider config、`runId`、`ModelCall`。                                                                                                         | 必须真实经过 `Model Gateway` 完成一次 provider 选择，并至少覆盖成功路径和 fallback 或 failure path；同时持久化 `ModelCall`、latency、token / cost 占位或真实值、`errorType`。                                                              | 可以压缩 provider 数量、路由规则复杂度和自动优化分支，不压缩真实路由与失败处理动作。                                     | 不得在 route、service、LangGraph node 或前端中直接散写 provider 调用。                                                                          | `AGENTS.md`、`docs/architecture.md`、`docs/runtime-lifecycle.md`、`packages/contracts/schemas/analysis/**`                     | `#159` `#160` `#165`                      |
| `Tool Registry（工具注册表，统一管理工具 schema、权限、风险等级、handler 和审计记录）/ Tool Governance（工具治理）`                 | 统一管理工具 schema、权限、风险等级、handler、调用记录和审计。                                                                                                                  | `Tool Registry`、tool schema、`riskLevel`、`permission`、handler、`tool_calls`。                                                                                                | 必须真实完成 tool schema 校验、权限 / 风险判断、handler 归口执行，并持久化 `ToolCall` 与 trace / audit 绑定。                                                                                                                              | 可以压缩工具数量、工具种类和外部插件来源，不压缩真实校验与执行动作。                                                     | 不得让模型或业务 service 直接调用工具 handler。                                                                                                 | `AGENTS.md`、`docs/architecture.md`、`packages/contracts/schemas/analysis/**`、`packages/contracts/schemas/model-tools/**`     | `#159` `#160`                             |
| `Human-in-the-loop（人工确认）/ ApprovalRequest（审批请求）`                                                                        | 高风险动作不能直接执行，需要进入 waiting 状态并等待人工确认。                                                                                                                   | `approval_requests`、waiting status、approval / reject event、`ApprovalRequest`。                                                                                               | 必须真实进入 `waiting` 状态，并在 approve / reject 后继续或终止；不能只是建 `approval_requests` 表。                                                                                                                                       | 可以压缩审批层级、审批人数量和组织流程，不压缩真实等待与决策动作。                                                       | 不得把高风险动作直接落地执行后再补审批记录。                                                                                                    | `docs/runtime-lifecycle.md`、`packages/contracts/schemas/analysis/**`                                                          | `#159` `#160`                             |
| `Knowledge（知识）/ RAG（Retrieval-Augmented Generation，检索增强生成）/ SourceEvidence（来源证据）`                                | 把业务文档、知识切片和向量检索结果转为可追溯证据。                                                                                                                              | `LlamaIndex`（知识解析、索引与检索编排层）、`Milvus / Milvus Lite`（向量库；preview 阶段由 `Milvus Lite` 承载）、`knowledge_documents`、`knowledge_chunks`、`source_evidence`。 | 必须真实完成 document chunk、embedding、向量检索，并把结果标准化绑定为 `SourceEvidence`；preview 阶段允许由 `Milvus Lite` 完成 retrieval，不能只把文本塞进回答。                                                                           | 可以压缩文档数量、索引规模和召回策略分支，不压缩真实检索与证据绑定动作。                                                 | 不得把 raw retrieval output 直接塞给 UI，不得绕过 `SourceEvidence` 标准对象。                                                                   | `docs/architecture.md`、`docs/database.md`、`packages/contracts/schemas/analysis/**`                                           | `#160` `#165`                             |
| `Message / MessageStream / SSE（Server-Sent Events，服务端事件流）`                                                                 | assistant 输出可以流式返回，并能落库回放。                                                                                                                                      | `messages`、`message_streams`、SSE endpoint、replay endpoint。                                                                                                                  | 必须真实流式输出、持久化 stream chunks，并支持 replay；不能只返回一次性 assistant 文本。                                                                                                                                                   | 可以压缩并发客户端数量、会话数量和协同分支，不压缩真实流式传输与回放动作。                                               | 不得用 `RunEvent` 承载 token delta，不得把 `stream.completed` 当作 `run.completed`。                                                            | `docs/runtime-business-integration.md`、`docs/runtime-lifecycle.md`、`packages/contracts/schemas/analysis/**`                  | `#158` `#161` `#162`                      |
| `Report / Decision（报告 / 决策）`                                                                                                  | 一次 run 不只输出聊天文本，还要产出报告、决策和行动建议。                                                                                                                       | `reports`、`report_sections`、`decisions`、`action_suggestions`。                                                                                                               | 必须真实产出结构化 `Report / Decision`，并关联 evidence；不能把 raw model output 直接当正式报告。                                                                                                                                          | 可以压缩报告章节数量、决策流程复杂度和衍生动作分支，不压缩真实结构化产物动作。                                           | 不得把 raw model output 直接当成正式报告对象。                                                                                                  | `docs/runtime-l3-golden-path.md`、`packages/contracts/schemas/reports/**`                                                      | `#160` `#162`                             |
| `Feedback / BadCase / Evaluation（反馈 / 坏例 / 评估）`                                                                             | 用户反馈可以沉淀为坏例或评估入口，用于后续质量改进。                                                                                                                            | `feedback`、`bad_cases`、`evaluation_runs`、`evaluation_scores`、`DeepEval`（评估框架）/ `RAGAs`（RAG 质量评估框架）。                                                          | 必须真实从 `Feedback` 形成 `BadCase` 或 `Evaluation entry`，并能回挂到 `runId`；不能只保留一个反馈按钮或静态入口。                                                                                                                         | 可以压缩 case 数量、评估数据集规模和后处理分支，不压缩真实回挂与沉淀动作。                                               | 不得把 `Feedback`、`Memory`、`Evaluation` 混成一个对象或一条写链。                                                                              | `AGENTS.md`、`docs/runtime-lifecycle.md`、`packages/contracts/schemas/feedback/**`、`packages/contracts/schemas/evaluation/**` | `#163` `#165`                             |
| `Observability（可观测性，用来追踪一次运行从输入、模型调用、工具调用到报告产出的完整链路）/ Trace（调用链追踪）/ Audit（审计）`     | 一次 run 从输入、模型调用、工具调用、RAG、证据、报告到错误都能被追踪。                                                                                                          | `run_events`、`model_calls`、`tool_calls`、error records、`LangSmith`（链路调试与评估观测服务）或 `Langfuse`（调用追踪与观测服务）。                                            | 必须真实用 `runId` 串起 model call、tool call、RAG retrieval、`SourceEvidence`、report 和 error / audit 记录；不能只依赖本地日志。                                                                                                         | 可以压缩观测 provider 数量、仪表盘规模和扩展分析分支，不压缩真实链路归集动作。                                           | 不得把观测信息只留在本地日志，不回挂正式运行对象。                                                                                              | `docs/architecture.md`、`docs/runtime-lifecycle.md`、`docs/deployment.md`                                                      | `#159` `#160` `#161` `#165`               |
| `Cost / Latency / Error Classification（成本 / 延迟 / 错误分类）`                                                                   | 记录模型和工具调用的成本、耗时和错误类型，用于排障和后续优化。                                                                                                                  | `model_calls`、`tool_calls`、`run_events`、error records。                                                                                                                      | 必须真实记录 latency 和 `errorType`；token / cost 至少有正式字段或明确来源，不能藏在无类型 `metadata`。                                                                                                                                    | 可以压缩成本拆分维度、报表粒度和自动优化分支，不压缩真实记录动作。                                                       | 不得把成本和错误只放进无类型 `metadata`。                                                                                                       | `docs/runtime-lifecycle.md`、`packages/contracts/schemas/analysis/**`                                                          | `#160` `#165`                             |
| `Load Test（小规模负载测试）/ Failure Simulation（故障模拟）`                                                                       | 验证健康检查、run 创建、SSE、模型失败、工具超时、SQL Guard 拒绝等基础风险。                                                                                                     | `scripts/load`、`scripts/failure-simulation`、`scripts/smoke`。                                                                                                                 | 必须有可执行脚本触发基础路径或故障路径；不能只在文档中声明存在演练。                                                                                                                                                                       | 可以压缩并发规模、故障场景数量和长尾分支，不压缩可执行脚本动作。                                                         | 不得只靠口头说明存在故障演练，不得让 smoke 长期停留在 echo placeholder。                                                                        | `docs/deployment.md`、`scripts/**`、`docs/runtime-l3-golden-path.md`                                                           | `#161` `#164`                             |
| `Deployment（部署）/ Single ECS Docker Runtime（单机 ECS Docker 预览运行时）/ Resettable Preview（可重置预览环境）`                 | 前端静态资源通过 ECS 上的 `Caddy / Nginx` 托管，后端 runtime / worker 与 `MySQL / Redis / Milvus Lite` 通过同一台 ECS Docker Compose 承载，preview 环境可以重置并通过脚本恢复。 | `deploy/docker`、`scripts/build`、`scripts/package`、`scripts/deploy`、`scripts/smoke`、`scripts/rollback`、`database/mysql/**`。                                               | 必须能在单台 ECS 上真实承载 frontend static hosting、`agent-runtime`、`agent-worker`、`MySQL 8.x`、`Redis`、`Milvus Lite`，`health / smoke` 可运行，preview reset 通过 `migration / seed / query verify`；不能依赖手工控制台或散操作证明。 | 可以压缩环境数量、数据规模和发布流程层级，但不压缩前端托管、后端运行、数据库、缓存、向量检索、smoke 与恢复这些核心动作。 | 不得把 `CloudBase Run / CloudBase Pages / OSS` 当作当前 preview 主线，不得把 `MySQL / Redis / Milvus Lite` 变成“后补”，不得手工改表后反推代码。 | `docs/deployment.md`、`docs/database.md`、`deploy/**`、`database/mysql/**`                                                     | `#157` `#158` `#161` `#164`               |
| `Memory（记忆）/ L4 Hooks（L4 预留钩子）`                                                                                           | L3 先记录足够的反馈、评估、错误、成本和审计数据，为后续优化提供输入。                                                                                                           | Memory 相关表或计划对象、`ContextSnapshot`、`PermissionSnapshot`、`CostRecord`、`ErrorRecord`、`RunAuditRecord`。                                                               | 必须真实记录后续优化所需的 `feedback / evaluation / error / cost / audit` 输入，并回挂 `runId`；L3 不做自动优化。                                                                                                                          | 可以压缩 snapshot 种类、hook 数量和后续优化分支，不压缩真实记录与回挂动作。                                              | 不得为了未来优化另起第二套 lifecycle 或把关键对象藏进 metadata。                                                                                | `docs/runtime-lifecycle.md`、`docs/contracts.md`、`packages/contracts/**`                                                      | `#163` `#165`                             |

## 3. Preview 部署优先策略

- 当前 `#155` 以 `Single ECS Docker Runtime` 作为 preview 真实集成环境主线。
- 本地只要求 `Docker build`、基础测试、contract check、smoke client。
- 本地完整 MySQL / Redis / Milvus 复刻不是当前阻塞项。
- Preview 数据库允许 reset。
- reset 必须通过 `migration -> seed -> query verify`。
- 不能用 Navicat 或控制台手工改表结构后反推代码。
- 前端静态资源通过 ECS 上的 `Caddy / Nginx` 托管，后端 runtime / worker 通过同一台 ECS Docker Compose 部署。
- `MySQL / Redis / Milvus Lite` 在 ECS preview runtime 中承载，核心运行动作不可压缩。
- 当前 preview 主线不是 `CloudBase Run / CloudBase Pages / OSS`。
- `CloudBase Run / CloudBase Pages / CloudBase SQL` 只保留为历史验证资源或后续可选平台。
- `CloudBase Functions` 不作为当前主部署链路。

## 4. 后续 PR 使用方式

后续 `#155` 子任务 PR 必须在 PR body 或执行报告中说明：

- 本 PR 覆盖了本文件中的哪些能力项。
- 哪些能力已经实现。
- 哪些仅完成承载位。
- 本 PR 是否已经体现相关能力的核心运行动作；如果仅完成承载位，必须说明核心动作由哪个后续 issue 完成。
- 哪些是范围压缩，不是能力缺失。
- 使用了哪些事实源。
- 跑了哪些验证。
- 是否涉及 contracts / migration / deployment / env 变化。
- 是否存在 `mock / real` 双轨风险。

如果某项能力本轮只完成承载位，也必须明确写出承载位已经落在哪里，以及仍未实现的是哪一段运行链路。

## 5. 非目标

以下内容不要求在一个 PR 中一次性完成：

- 一次性实现所有 runtime 能力。
- 一次性完成全部 provider 接入。
- 一次性完成完整外部插件市场。
- 一次性完成完整审批系统。
- 一次性完成生产级高并发压测。
- 一次性完成 L4 自动优化。

这些非目标不代表可以绕过正式承载位，也不代表后续可以用散实现替代正式边界。
